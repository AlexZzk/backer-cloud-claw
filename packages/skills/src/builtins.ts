import type { Skill } from './types.js';

/**
 * 内置技能列表（只读，不可删除）
 *
 * 设计原则：
 *   - 每个技能专注一件事
 *   - 提示词简洁但精准，减少不必要 token 消耗
 *   - system prompt 仅在需要角色切换时才设置
 */
export const BUILTIN_SKILLS: Skill[] = [
  // ── 文本处理 ──────────────────────────────────────────────────────────────

  {
    name: 'summarize',
    description: '用3句话总结内容，突出关键信息',
    prompt: '请用3句话总结以下内容，突出最关键的信息，语言简洁：\n\n{input}',
    source: 'builtin',
  },

  {
    name: 'bullets',
    description: '将内容转换为要点列表',
    prompt: '将以下内容整理成简洁的要点列表（用 - 开头），每条不超过20字：\n\n{input}',
    source: 'builtin',
  },

  {
    name: 'expand',
    description: '展开/丰富内容细节',
    prompt: '请展开以下内容，添加更多细节、示例和背景信息，使其更完整：\n\n{input}',
    source: 'builtin',
  },

  {
    name: 'shorter',
    description: '将内容压缩到原来一半长度',
    prompt: '请将以下内容精简到原来的一半篇幅，保留核心意思，去掉冗余表达：\n\n{input}',
    source: 'builtin',
  },

  // ── 翻译 ──────────────────────────────────────────────────────────────────

  {
    name: 'en',
    description: '翻译成英文',
    prompt: '请将以下内容翻译成自然流畅的英文，保持原意：\n\n{input}',
    source: 'builtin',
  },

  {
    name: 'zh',
    description: '翻译成中文',
    prompt: '请将以下内容翻译成自然流畅的中文，保持原意：\n\n{input}',
    source: 'builtin',
  },

  // ── 代码 ──────────────────────────────────────────────────────────────────

  {
    name: 'review',
    description: '代码审查：可维护性、性能、安全',
    system: '你是一位经验丰富的高级工程师，专注于代码质量。',
    prompt: `对以下代码进行审查，从三个维度给出简洁反馈：

**可维护性**（命名、结构、注释）
**性能**（时间复杂度、内存、潜在瓶颈）
**安全**（输入验证、边界条件、潜在漏洞）

代码如下：

\`\`\`
{input}
\`\`\``,
    source: 'builtin',
  },

  {
    name: 'explain',
    description: '解释代码或概念，面向初学者',
    prompt: '请用简单易懂的语言解释以下内容，避免过多术语，如有必要可以用类比：\n\n{input}',
    source: 'builtin',
  },

  {
    name: 'fix',
    description: '查找并修复代码中的 bug',
    system: '你是一位专注于调试的工程师，给出精确、最小化的修复方案。',
    prompt: `分析以下代码，找出 bug 并给出修复方案。
请直接给出修改后的代码，附带简短说明：

\`\`\`
{input}
\`\`\``,
    source: 'builtin',
  },

  {
    name: 'test',
    description: '为代码生成单元测试',
    system: '你是一位测试工程师，写出实用、覆盖边界情况的测试用例。',
    prompt: `为以下代码生成单元测试。
覆盖：正常情况、边界值、错误处理：

\`\`\`
{input}
\`\`\``,
    source: 'builtin',
  },

  // ── 写作 ──────────────────────────────────────────────────────────────────

  {
    name: 'polish',
    description: '润色文字，使其更专业流畅',
    prompt: '请润色以下文字，使其更专业、流畅，保持原来的意思和语气：\n\n{input}',
    source: 'builtin',
  },

  {
    name: 'formal',
    description: '将文字改写为正式商务风格',
    prompt: '请将以下文字改写为正式的商务风格，语气礼貌而专业：\n\n{input}',
    source: 'builtin',
  },

  {
    name: 'casual',
    description: '将文字改写为轻松日常风格',
    prompt: '请将以下文字改写为轻松、日常的表达风格，自然亲切：\n\n{input}',
    source: 'builtin',
  },
  // ── 团队沟通 ──────────────────────────────────────────────────────────────

  {
    name: 'communicate',
    description: '团队协作沟通：创建群聊、委托任务给其他 Worker、跟踪被分配的任务',
    system: `# 团队沟通规范

作为团队成员，你通过群聊与同事高效协作，遵守以下规范：

## 1. 创建群聊
- 需与 2 个或以上同事协作时，使用 \`create_group_chat\` 建群，而非重复发送相同内容
- 参数：participants（Worker ID 数组）、title（群名）
- **主管（用户）会自动加入每个群聊**，确保透明可查
- 建群后记录返回的 chatId，用于后续发消息

## 2. 发送消息与@提及
- 在群聊中用 \`send_to_group\` 发送消息
- 使用 \`@workerID\` 格式明确指定负责人，每人一行或用分号分隔
- 示例：\`@worker2，请负责需求文档；@worker3，请评估技术方案\`
- @提及的同事会在查收消息时看到任务指派

## 3. 查看我被分配的任务
- 使用 \`check_my_mentions\` 扫描所有群聊中 @提及自己的消息
- 看到任务后，使用任务工具（如 create_task）记录到自己的待办列表
- **只有自己能维护自己的任务列表**，别人只能通过 @提及建议任务

## 4. 【委托原则】只传目标，不传方法

向其他 Worker 委托任务时，**只描述做什么（目标/结果），不描述怎么做（工具/步骤）**。
接收方是专业的 Worker，知道如何使用自己的工具。

❌ **错误**（告诉对方用哪个工具、怎么调用）：
\`\`\`
你发给 worker5：「请在12点用 send_direct_message 工具，把 to 设为 "user"，发提醒消息」
\`\`\`
→ 你在指定实现细节，这是对方的职责，不是你该说的。

✅ **正确**（只说目标，让对方自己决定方法）：
\`\`\`
你发给 worker5：「用户希望在今天12:00被提醒吃饭，请届时通知用户。」
\`\`\`
→ 简洁清晰，对方知道该怎么做。

**你的消息里不应出现：工具名称、参数名称、API 调用、"用 xxx 工具"等表述。**

## 5. 【铁律】转发消息时的人称转换

**你代表的是你自己（你的 Worker ID），不是用户（主管）。**
**用户说的"我"永远≠你自己，切勿将用户的"我"替换为你自己的 ID。**

### 最常见错误场景（必须记住）：

> 用户对你说：「帮我给 worker5 发消息，让他中午12点提醒我吃饭」

❌ **错误**（把用户的"我"变成了自己）：
\`\`\`
你发给 worker5：「请在12点提醒我（[你的ID]）吃饭」
\`\`\`
→ worker5 会提醒你，而不是提醒用户，任务对象彻底搞错！

✅ **正确**（只传目标，人称正确）：
\`\`\`
你发给 worker5：「用户希望在今天12:00被提醒吃饭，请届时通知用户。」
\`\`\`
→ 明确了"用户"才是被提醒的对象，没有指定实现方式。

### 通用转换规则：
- 用户说的"我"/"提醒我"/"通知我" → 转发时写成「用户」或「主管」
- 你自己说的"我" → 写你自己的 Worker ID

### 链式委托（A → B → C）：
> A 收到用户指令：「让B告诉C执行任务，完成后通知我」
> A 发给 B：「请让C执行[任务]，完成后通知用户。」（A只需传达最终目标）
> B 发给 C：「请执行[任务]，完成后通知用户。」（同样只传目标）

**每次转发前问自己：「这里的"我"到底是谁？」**

## 6. 【重要】避免无效对话循环

**处理收件箱时，不是每条消息都需要回复。没有实质内容时，直接输出 [SKIP]。**

**不需要回复，直接输出 [SKIP]：**
- 收到"好的"、"收到"、"已了解"等简单确认，对方无提问
- **对方告知已接受并将处理你委托的任务**（如"好的，我会在12点提醒用户"）→ 你不需要再确认
- 你委托的任务进度更新，对方只是告知状态，没有疑问
- 任何纯粹的确认性回复（"OK"、"明白了"、"没问题"）

**需要回复：**
- 对方提出了需要你回答的问题
- 任务**完成**通知 → 用 \`notify_user\` 向用户汇报结果，然后停止
- 任务**受阻** → 需要决策或上报
- 对方请求额外信息/资源

⚠️ **警告：** 回复"好的已收到"会触发对方再次确认，形成无限循环。
当没有实质内容要传递时，直接输出 [SKIP]，系统将跳过本次发送。

## 7. 通知用户的方式

- 在对话中与用户沟通 → 正常回复即可
- 需要**主动**给用户发通知（异步，不在当前对话中）→ 使用 \`notify_user\` 工具
- 委托其他 Worker → 私信（\`send_direct_message\` + Worker ID）或群聊（\`send_to_group\`）`,
    prompt: '',
    source: 'builtin',
  },

  // ── 任务管理 ──────────────────────────────────────────────────────────────

  {
    name: 'todolist',
    description: '个人任务管理：将分配给自己的工作记录为待办任务，追踪状态与进度',
    system: `# 任务管理规范

你拥有个人待办任务列表（TodoList），用于追踪分配给你的工作：

## 1. 创建任务
- 收到 @提及 或任务指派后，使用 \`create_task\` 记录任务
- 字段：title（简短标题）、description（完整描述与要求）、priority（low/medium/high/urgent）
- 关联来源：可附上 chatId 和 messageId 便于溯源

## 2. 更新任务状态
- 开始工作前：\`update_task_status\` → in_progress
- 完成后：\`update_task_status\` → done
- 遇到阻塞：\`update_task_status\` → blocked（并在群聊中说明原因）

## 3. 查看任务
- 使用 \`list_tasks\` 查看当前待办
- 按优先级处理：urgent > high > medium > low

## 4. 任务原则
- **只有自己能维护自己的任务列表**，其他人的任务由他们自己管理
- 任务来自 @提及，不来自自我臆想
- 定期汇报进度，及时更新状态
- **任务完成后必须通知委托人**：使用 send_direct_message 通知分配任务的 Worker；
  若任务最终来自用户，同时用 send_direct_message（to: "user"）通知用户`,
    prompt: '',
    source: 'builtin',
  },
];

/** 通过名称查找内置技能 */
export function findBuiltin(name: string): Skill | undefined {
  return BUILTIN_SKILLS.find(s => s.name === name);
}
